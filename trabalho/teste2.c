#include <stdio.h> 
#include <stdlib.h>

int main (void) {

    int opcao = 0;

    while (opcao != 5);
        system("clear");
        printf("Bem vindo a Forca!");
        printf("Menu principal");
        printf("Escolha a quantidade de caracteres da sua palavra: ");
        


    return 0;
}